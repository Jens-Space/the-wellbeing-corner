[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional: Static export settings (if using static export)
# [context.production.environment]
#   NEXT_TELEMETRY_DISABLED = "1"
